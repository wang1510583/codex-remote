[Service]
# Start the long-lived shared daemon before Codex Remote. The command is
# idempotent and returns immediately when the daemon is already listening.
ExecStartPre=/home/wangjiafeng/.local/bin/codex app-server daemon start --enable realtime_conversation
